import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'leave-layout',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './index.component.html',
})
export class LeaveLayout {}
